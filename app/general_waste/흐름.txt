📦 GENERAL_WASTE 기능 흐름 정리

────────────────────────────────────────
 일반 촬영 흐름
────────────────────────────────────────

1) 카메라 촬영 페이지 (page.tsx)
   - useGeneralCapture로 사진 촬영
   - base64 → localStorage(wasteImage) 저장
   - /general_waste/analyze 페이지 이동

2) 분석 페이지 (analyze/page.tsx)
   - useGeneralAnalyze가 자동 실행
   - wasteImage 또는 text 기반 분석
   - analyzeWaste API 호출 후 결과 반환
   - /general_waste/result 로 이동

3) 결과 페이지 (result/page.tsx)
   - useWasteResult로 사진 + content 로드
   - 분석 결과 출력
   - 다시 촬영하기 버튼 제공
   

────────────────────────────────────────
 QR 업로드 흐름
────────────────────────────────────────

1) QR 페이지 (qr/page.tsx)
   - /recycle/reset ← 서버 업로드 상태 초기화
   - QRCodeCanvas로 QR 표시
   - /recycle/check ← 1초 주기로 polling
   - 업로드되면 wait 페이지로 이동

2) 휴대폰 업로드 페이지 (mobile_upload/page.tsx)
   - 사진 파일 선택 → base64 변환
   - /recycle/mobile-upload 로 전송

3) wait 페이지 (wait/page.tsx)
   - useWaitForUpload 훅 실행
   - /recycle/check polling
   - 업로드 ID 감지 시 /general_waste/analyze?id=XX 이동

4) 분석 페이지 (analyze/page.tsx)
   - id가 있는 경우 → getQRImage(id)로 base64 받아옴
   - 분석 후 result 페이지 이동

5) 결과 페이지 (result/page.tsx)
   - 사진 + 분석 결과표시
   - 다시 촬영하기 이동 가능


────────────────────────────────────────
 폴더 구조
────────────────────────────────────────

general_waste/
 ├─ page.tsx              ← 촬영 화면
 ├─ qr/page.tsx           ← QR 생성 화면
 ├─ mobile_upload/page.tsx← 휴대폰 사진 업로드
 ├─ wait/page.tsx         ← 업로드 대기
 ├─ analyze/page.tsx      ← 분석 중 화면
 └─ result/page.tsx       ← 분석 결과 화면

────────────────────────────────────────
 전체 흐름 정리
────────────────────────────────────────

[촬영 → 분석 → 결과]
[QR → 업로드 → wait → 분석 → 결과]
두 흐름 모두 result 페이지에서 합류.
